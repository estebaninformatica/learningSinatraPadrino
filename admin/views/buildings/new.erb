<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:buildings, :index) %></li>
  <li class="active"> <%= link_to tag_icon(:plus, pat(:new)), url(:buildings, :new) %></li>
</ul>
<div class="tabs-content">
  <% form_for :building, url(:buildings, :create), :class => 'form-horizontal',:role="form" do |f| %>
    <%= partial 'buildings/form', :locals => { :f => f } %>
 <% end %>
</div>
